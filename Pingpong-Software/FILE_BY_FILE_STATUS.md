# FILE_BY_FILE_STATUS.md

Generated by static require-graph analysis (BFS from all real entry points: `server.js`, `scripts/*.js` invoked by npm scripts, `test/run-all.js`, `ecosystem.config.js`) cross-checked against actual `<script>` tags in `admin/index.html` / `public/index.html`, and independently re-run test suites. 146 original JS files inspected, all pass `node --check` (0 syntax errors).

Legend: **Complete** = wired and used at runtime. **PARTIAL** = exists, works, but not fully integrated (either by design or by gap). **MISSING** = built but never connected to anything.

---

## UPDATE (2026-08-10, second pass) — status changes from actual fixes made

| File | Previous status | New status | What changed |
|---|---|---|---|
| `integration_update/middleware/index.js` | PARTIAL (built, never required) | **Complete** | Now `require()`'d in `server.js` and its `requestId()` helper wired as global HTTP middleware (correlation IDs on every response). |
| `ai/ai-recovery.js` | MISSING (dead code) | **Complete** | Now `require()`'d in `ai/ai-service.js`; `retryJob()` wraps the one external AI provider call (transient-failure retry, bounded at 2 retries). |
| `test/run-all.js` | N/A | **Modified** | Now also discovers and runs `integration_update/country_permission/test/`, `integration_update/merchant/test/`, and `integration_update/module4_wallet_ledger/test/` (94 additional assertions), closing the CI blind spot. |
| `integration_update/admin_updates/index.js` | Misclassified as DEAD/ORPHAN in first pass | **PARTIAL (by design, correctly)** | Read in full: self-documented as an additive introspection manifest with no current UI consumer. Left unwired — wiring it without a consuming "installed extensions" admin screen would add surface area with no behavior change. Not a bug. |
| `integration_update/api/index.js` | Misclassified as DEAD/ORPHAN in first pass | **PARTIAL (by design, correctly)** | Same reasoning — explicitly documented as "not used today," additive convenience for a future multi-module attach pattern. Left as-is. |
| `test/productionAuditWiring.test.js` | N/A (new file) | **Complete** | New regression suite (8 assertions) covering both wiring changes above: retry-then-succeed, retry-exhausted-then-reject, correlation ID generation, and correlation ID passthrough from an incoming header. |
| `scripts/wallet-opening-balance-migration.js` | N/A (new file) | **Complete (tooling), cutover itself NOT executed** | Idempotent, dry-run-by-default migration script from legacy `data/users.json` to Module 4's Postgres ledger. Verified with mock data. Requires a real Postgres instance to actually execute — see `WALLET_CUTOVER_PLAN.md`. |

**Unchanged, explicitly not touched this pass** (verified safe/correct as-is, not modified):
- `integration_update/module4_wallet_ledger/**` — remains intentionally unwired (`MODULE4_WALLET_ENABLED=false`), per its own documented design and the explicit no-destructive-change rule. See `WALLET_CUTOVER_PLAN.md`.
- All room/seat/voice/SFU/Socket.IO logic — not modified in this pass (see main audit report for what was reviewed vs. what still needs a dedicated phase pass).

## (root)/

| File | Purpose | Status |
|---|---|---|
| `./agencyApproval.js` | ================================================== PHASE 6 — AGENCY APPROVAL WORKFLOW (item 1 of Phase 6) ================================== | WIRED — reachable from server.js — Complete |
| `./agencyHost.js` | ================================================== PHASE 3 — AGENCY & HOST SYSTEM ================================================== Additiv | WIRED — reachable from server.js — Complete |
| `./analyticsHub.js` | ================================================== PHASE 9 — ANALYTICS HUB (minimal additive backend) ====================================== | WIRED — reachable from server.js — Complete |
| `./approvalEngine.js` | ================================================== PHASE 6 — SHARED APPROVAL ENGINE ================================================== One g | WIRED — reachable from server.js — Complete |
| `./badges.js` | badges.js ================================================== PREMIUM BADGE SYSTEM (Blue Diamond V, additive) =============================== | WIRED — reachable from server.js — Complete |
| `./banManagement.js` | ================================================== PHASE 8 — BAN MANAGEMENT MODULE ================================================== Same s | WIRED — reachable from server.js — Complete |
| `./bannerManagement.js` | ================================================== Home Banner System — Backend Core (additive module) ===================================== | WIRED — reachable from server.js — Complete |
| `./callHosting.js` | ================================================== CALL HOSTING SYSTEM (additive module) ==================================================  | WIRED — reachable from server.js — Complete |
| `./callSignaling.js` | callSignaling.js ================================================== PRIVATE INBOX AUDIO/VIDEO CALLING — signaling only ===================== | WIRED — reachable from server.js — Complete |
| `./coinCenter.js` | ================================================== Admin Coin Center — Backend Core (additive module) ====================================== | WIRED — reachable from server.js — Complete |
| `./coinSellers.js` | ================================================== WALLET — COIN SELLER LIST ================================================== Replaces the | WIRED — reachable from server.js — Complete |
| `./countries.js` | ========================================================================== PingPong — Country / Language catalogue | WIRED — reachable from server.js — Complete |
| `./diamondSeller.js` | ================================================== PHASE 6 — DIAMOND SELLER MODULE (item 11 of the dev sequence) =========================== | WIRED — reachable from server.js — Complete |
| `./ecosystem.config.js` | PM2 process manager configuration.  Usage:   pm2 start ecosystem.config.js --env production   pm2 save                # persist the process  | WIRED — reachable from server.js — Complete |
| `./framesApproval.js` | ================================================== PHASE 6 — FRAMES APPROVAL WORKFLOW (item 9 of the dev sequence) ========================= | WIRED — reachable from server.js — Complete |
| `./fruitWheelAudit.js` | fruitWheelAudit.js Persistent, append-only audit trail for the Fruit Wheel game.  Why this exists: the in-memory game engine in server.js al | WIRED — reachable from server.js — Complete |
| `./giftsApproval.js` | ================================================== PHASE 6 — GIFTS APPROVAL WORKFLOW (item 10 of the dev sequence) ========================= | WIRED — reachable from server.js — Complete |
| `./health-check.js` | health-check.js ================================================== PHASE 1 (Tier A) — HEALTH CHECK SERVICE ================================= | WIRED — reachable from server.js — Complete |
| `./idLevel.js` | ================================================== ID Level System (1–N) — Backend Core (additive module) ================================== | WIRED — reachable from server.js — Complete |
| `./mobileMigration.js` | ================================================== MOBILE ACCOUNT MIGRATION + DUPLICATE MERGE (Production Hotfix, Firebase build) ========== | WIRED — reachable from server.js — Complete |
| `./namefxApproval.js` | ================================================== PHASE 6 — NAME EFFECTS APPROVAL WORKFLOW (item 8 of the dev sequence) =================== | WIRED — reachable from server.js — Complete |
| `./rbac.js` | ========================================================================== PingPong RBAC (Role Based Access Control) — Enterprise Admin Laye | WIRED — reachable from server.js — Complete |
| `./rechargeWithdrawApproval.js` | ================================================== PHASE 6 — RECHARGE / WITHDRAW APPROVAL WORKFLOW (item 2 of Phase 6 SRS, step 7 of the dev | WIRED — reachable from server.js — Complete |
| `./room-recovery.js` | room-recovery.js ================================================== PHASE 1 (Tier A) — ROOM RECOVERY MANAGER (scoped) ====================== | WIRED — reachable from server.js — Complete |
| `./server.js` | AUDIT FIX (Final integration sweep, 2026-07-29): every actual await in this codebase was already individually wrapped in try/catch (verified | WIRED — reachable from server.js — Complete |
| `./svip.js` | ================================================== SVIP Privilege System — Backend Core (additive module) ================================== | WIRED — reachable from server.js — Complete |
| `./turn-config.js` | turn-config.js ================================================== PHASE 1 (Tier A) — TURN SERVER INTEGRATION =============================== | WIRED — reachable from server.js — Complete |
| `./vehicles.js` | ================================================== VEHICLE ENTRY SYSTEM (Add-on Module) ================================================== P | WIRED — reachable from server.js — Complete |
| `./vipApproval.js` | ================================================== PHASE 6 — VIP MODULE (item 12 of the dev sequence — last approval domain) =============== | WIRED — reachable from server.js — Complete |
| `./voice-health.js` | voice-health.js ================================================== PHASE 1 (Tier A) — VOICE HEALTH MONITOR ================================= | WIRED — reachable from server.js — Complete |
| `./voice-reconnect.js` | voice-reconnect.js ================================================== PHASE 1 (Tier A) — SMART AUTO RECONNECT (server-side complement) ===== | WIRED — reachable from server.js — Complete |

## admin/

| File | Purpose | Status |
|---|---|---|
| `./admin/app.js` | ========================================================================== PingPong Control — Admin website logic. | WIRED (frontend <script> tag, not Node require) — Complete |

## ai/

| File | Purpose | Status |
|---|---|---|
| `./ai/ai-analytics.js` | ai/ai-analytics.js Simple counters other ai/ modules bump. Resets on server restart — if you want these to survive restarts, swap the in-mem | WIRED — reachable from server.js — Complete |
| `./ai/ai-chat.js` | ai/ai-chat.js Owns the "PingPong Help" support account: per-user session memory (in memory only, resets on server restart — this is intentio | WIRED — reachable from server.js — Complete |
| `./ai/ai-config.js` | ai/ai-config.js Single place every other ai/ module reads settings from. Nothing outside this file should ever read process.env directly for | WIRED — reachable from server.js — Complete |
| `./ai/ai-dashboard.js` | ai/ai-dashboard.js Exports a function that takes the existing requireAdmin middleware (from server.js) and returns an Express router — kept  | WIRED — reachable from server.js — Complete |
| `./ai/ai-logger.js` | ai/ai-logger.js Every AI action gets one line here: timestamp, which module, what it did, and the result. This is what powers the "Recent Lo | WIRED — reachable from server.js — Complete |
| `./ai/ai-moderator.js` | ai/ai-moderator.js Structural spam/abuse heuristics — repeated characters, link floods, duplicate messages, and (new) a simple scam/fake-lin | WIRED — reachable from server.js — Complete |
| `./ai/ai-monitor.js` | ai/ai-monitor.js Real, lightweight monitoring — no external dependencies. Measures actual Node.js process memory, OS load average, and event | WIRED — reachable from server.js — Complete |
| `./ai/ai-recovery.js` | ai/ai-recovery.js Deliberately minimal. Per the spec's own "AI Permissions" section, the AI must never perform destructive operations automa | DEAD CODE — retryJob() helper built but never imported by any AI provider/module — MISSING |
| `./ai/ai-room-assistant.js` | ai/ai-room-assistant.js The AI's presence *inside* voice rooms (separate from ai-chat.js, which only owns the private "PingPong Help" inbox) | WIRED — reachable from server.js — Complete |
| `./ai/ai-security.js` | ai/ai-security.js Lightweight, dependency-free rate limiting. Tracks recent hit timestamps per key (e.g. "chat:<userId>" or "room:<roomId>:< | WIRED — reachable from server.js — Complete |
| `./ai/ai-service.js` | ai/ai-service.js Provider-agnostic layer. Nothing outside this file (ai-chat.js, ai-room-assistant.js, etc.) ever talks to Gemini/OpenAI dir | WIRED — reachable from server.js — Complete |
| `./ai/providers/gemini-provider.js` | ai/providers/gemini-provider.js The ONLY file in this project that knows Gemini's specific request/response shape. ai-service.js talks to ev | WIRED — reachable from server.js — Complete |
| `./ai/providers/openai-provider.js` | ai/providers/openai-provider.js Same contract as gemini-provider.js: generate(messages, systemPrompt) -> { text, usage }. Swapping AI_PROVID | WIRED — reachable from server.js — Complete |

## integration_update/

| File | Purpose | Status |
|---|---|---|
| `./integration_update/admin_updates/index.js` | (no header comment) | DEAD/ORPHAN — not required by server.js or anything else — MISSING |
| `./integration_update/api/index.js` | ========================================================================== API — Shared Route Index | DEAD/ORPHAN — not required by server.js or anything else — MISSING |
| `./integration_update/config/index.js` | (no header comment) | WIRED — reachable from server.js — Complete |
| `./integration_update/country_permission/countryRegistry.js` | ========================================================================== Country Permission — Registry | WIRED — reachable from server.js — Complete |
| `./integration_update/country_permission/filtering.js` | ========================================================================== Country Permission — Filtering | WIRED — reachable from server.js — Complete |
| `./integration_update/country_permission/index.js` | ========================================================================== Country Permission — Package Entry Point | WIRED — reachable from server.js — Complete |
| `./integration_update/country_permission/middleware.js` | ========================================================================== Country Permission — Middleware | WIRED — reachable from server.js — Complete |
| `./integration_update/country_permission/routes.js` | ========================================================================== Country Permission — Admin Routes | WIRED — reachable from server.js — Complete |
| `./integration_update/country_permission/store.js` | ========================================================================== Country Permission — Store | WIRED — reachable from server.js — Complete |
| `./integration_update/country_permission/test/countryPermission.test.js` | countryPermission.test.js Standalone verification harness — no npm deps, mocks a minimal rbac instance (same shape as the real rbac.js publi | BUILT & PASSES standalone, but NOT wired into npm test / CI — PARTIAL |
| `./integration_update/database/index.js` | ========================================================================== Database — Shared Migration Index | WIRED — reachable from server.js — Complete |
| `./integration_update/merchant/index.js` | ========================================================================== Merchant — Package Entry Point | WIRED — reachable from server.js — Complete |
| `./integration_update/merchant/registry.js` | ========================================================================== Merchant — Registry | WIRED — reachable from server.js — Complete |
| `./integration_update/merchant/routes.js` | ========================================================================== Merchant — Admin Routes | WIRED — reachable from server.js — Complete |
| `./integration_update/merchant/store.js` | ========================================================================== Merchant — Store | WIRED — reachable from server.js — Complete |
| `./integration_update/merchant/test/merchant.test.js` | merchant.test.js Standalone verification harness — no npm deps, mocks a minimal rbac instance (same shape as the real rbac.js public API, sa | BUILT & PASSES standalone, but NOT wired into npm test / CI — PARTIAL |
| `./integration_update/middleware/index.js` | (no header comment) | DEAD/ORPHAN — not required by server.js or anything else — MISSING |
| `./integration_update/module4_wallet_ledger/index.js` | ========================================================================== Module 4 — Wallet Ledger — Package Entry Point | INTENTIONALLY UNWIRED — documented deferred cutover, awaiting sign-off — PARTIAL (by design) |
| `./integration_update/module4_wallet_ledger/redis/connectionFactory.js` | module4/redis/connectionFactory.js ================================================== MODULE 4 — DEPLOYMENT-AGNOSTIC REDIS CONNECTION FACTOR | Part of Module 4 wallet package — inactive until index.js attach() is called — PARTIAL (by design) |
| `./integration_update/module4_wallet_ledger/redis/keyspace.js` | module4/redis/keyspace.js ================================================== MODULE 4 — CLUSTER-SAFE KEY NAMING ============================ | Part of Module 4 wallet package — inactive until index.js attach() is called — PARTIAL (by design) |
| `./integration_update/module4_wallet_ledger/redis/lock.js` | module4/redis/lock.js ================================================== MODULE 4 — DISTRIBUTED LOCK (single-instance today, Cluster-ready)  | Part of Module 4 wallet package — inactive until index.js attach() is called — PARTIAL (by design) |
| `./integration_update/module4_wallet_ledger/redis/roomState.js` | module4/redis/roomState.js ================================================== MODULE 4 — STEP 4.3: DISTRIBUTED ROOM STATE (infrastructure la | Part of Module 4 wallet package — inactive until index.js attach() is called — PARTIAL (by design) |
| `./integration_update/module4_wallet_ledger/redis/routing.js` | module4/redis/routing.js ================================================== MODULE 4 — STEP 4.2: CROSS-INSTANCE SOCKET ROUTING (Redis-author | Part of Module 4 wallet package — inactive until index.js attach() is called — PARTIAL (by design) |
| `./integration_update/module4_wallet_ledger/redis/userProfile.js` | module4/redis/userProfile.js ================================================== MODULE 4 — STEP 4.5: DISTRIBUTED USER PROFILE (read-through  | Part of Module 4 wallet package — inactive until index.js attach() is called — PARTIAL (by design) |
| `./integration_update/module4_wallet_ledger/test/extra_boundary_tests.js` | (no header comment) | BUILT & PASSES standalone, but NOT wired into npm test / CI — PARTIAL |
| `./integration_update/module4_wallet_ledger/test/mockPg.js` | Minimal in-memory mock of a node-pg Pool, JUST enough to exercise wallet/index.js's real query strings (BEGIN/COMMIT/ROLLBACK, the exact INS | Part of Module 4 wallet package — inactive until index.js attach() is called — PARTIAL (by design) |
| `./integration_update/module4_wallet_ledger/test/regression_tests.js` | (no header comment) | BUILT & PASSES standalone, but NOT wired into npm test / CI — PARTIAL |
| `./integration_update/module4_wallet_ledger/wallet/db.js` | module4/wallet/db.js ================================================== MODULE 4 — STEP 4.4: WALLET POSTGRES CONNECTION ==================== | Part of Module 4 wallet package — inactive until index.js attach() is called — PARTIAL (by design) |
| `./integration_update/module4_wallet_ledger/wallet/index.js` | module4/wallet/index.js ================================================== MODULE 4 — STEP 4.4: DISTRIBUTED WALLET LAYER =================== | Part of Module 4 wallet package — inactive until index.js attach() is called — PARTIAL (by design) |

## monitoring/

| File | Purpose | Status |
|---|---|---|
| `./monitoring/metrics.js` | Lightweight Prometheus exposition with zero runtime dependencies. Metrics are derived from existing application state; this module never mut | WIRED — reachable from server.js — Complete |

## perf/

| File | Purpose | Status |
|---|---|---|
| `./perf/cache.js` | ================================================== PHASE 11 — SHORT-TTL READ CACHE (additive) ============================================== | WIRED — reachable from server.js — Complete |
| `./perf/compression.js` | ================================================== PHASE 11 — RESPONSE COMPRESSION ================================================== Unlike | WIRED — reachable from server.js — Complete |
| `./perf/dataFolder.js` | ================================================== PHASE 12 — PERSISTENT DISK PATH RESOLUTION (additive) =================================== | WIRED — reachable from server.js — Complete |
| `./perf/dbPersistence.js` | ================================================== PHASE 12 — POSTGRES DURABILITY BACKSTOP (additive, optional) ============================ | WIRED — reachable from server.js — Complete |
| `./perf/userIndex.js` | ================================================== PHASE 11 — O(1) USER-ID INDEX (additive) ================================================ | WIRED — reachable from server.js — Complete |
| `./perf/writeQueue.js` | ================================================== PHASE 11 — DEBOUNCED DISK-WRITE QUEUE (additive) ======================================== | WIRED — reachable from server.js — Complete |

## public/

| File | Purpose | Status |
|---|---|---|
| `./public/app.js` | ========================================================================== PingPong frontend — talks to server.js exactly as written. | WIRED (frontend <script> tag, not Node require) — Complete |
| `./public/bannerSlider.js` | ================================================== Home Banner System — Banner Slider (Home page, user side) =============================== | WIRED (frontend <script> tag, not Node require) — Complete |
| `./public/emojiReaction.js` | ================================================================ Emoji Reaction feature — Lottie reaction popup + avatar overlay. Fully addi | WIRED (frontend <script> tag, not Node require) — Complete |
| `./public/firebaseClient.js` | ================================================== FIREBASE AUTH MIGRATION — PHASE C (client) ============================================== | WIRED (frontend <script> tag, not Node require) — Complete |
| `./public/i18n.js` | ========================================================================== PingPong — Runtime Localization (First Time Profile Setup: Sectio | WIRED (frontend <script> tag, not Node require) — Complete |
| `./public/voice-sfu.js` | public/voice-sfu.js ================================================== PHASE 3, STEP 3.3 — CLIENT-SIDE SFU (LIVEKIT) INTEGRATION =========== | WIRED (frontend <script> tag, not Node require) — Complete |

## redis/

| File | Purpose | Status |
|---|---|---|
| `./redis/cache.js` | redis/cache.js ================================================== PHASE 2A — REDIS CACHE LAYER ============================================= | WIRED — reachable from server.js — Complete |
| `./redis/client.js` | redis/client.js ================================================== PHASE 2A — REDIS CONNECTION MANAGER ===================================== | WIRED — reachable from server.js — Complete |
| `./redis/clusterRead.js` | redis/clusterRead.js ================================================== PHASE 2B-1 — CROSS-INSTANCE READ PATH ============================== | WIRED — reachable from server.js — Complete |
| `./redis/index.js` | redis/index.js ================================================== PHASE 2A/2B-1/2B-2 — SHARED STATE FOUNDATION (orchestrator) ============== | WIRED — reachable from server.js — Complete |
| `./redis/presence.js` | redis/presence.js ================================================== PHASE 2B-2 — GLOBAL PRESENCE & DISTRIBUTED USER STATE ================= | WIRED — reachable from server.js — Complete |
| `./redis/pubsub.js` | redis/pubsub.js ================================================== PHASE 2B-1 — CROSS-INSTANCE PUB/SUB (state-awareness channels) ========== | WIRED — reachable from server.js — Complete |
| `./redis/roomJoinRpc.js` | redis/roomJoinRpc.js ================================================== GAP #1 (Redis Authoritative Runtime State) — REMAINING ITEM 2: CROSS | WIRED — reachable from server.js — Complete |
| `./redis/roomOpRpc.js` | redis/roomOpRpc.js ================================================== GAP #2 (Cross-Instance Room Operation Forwarding) ==================== | WIRED — reachable from server.js — Complete |
| `./redis/roomState.js` | redis/roomState.js ================================================== PHASE 2A — SHARED ROOM STATE (mirror) ================================ | WIRED — reachable from server.js — Complete |
| `./redis/sessionStore.js` | redis/sessionStore.js ================================================== PHASE 2A — SHARED SESSION MANAGEMENT ============================== | WIRED — reachable from server.js — Complete |
| `./redis/socketAdapter.js` | redis/socketAdapter.js ================================================== PHASE 2B-1 — SOCKET.IO REDIS ADAPTER ============================= | WIRED — reachable from server.js — Complete |
| `./redis/userState.js` | redis/userState.js ================================================== PHASE 2A — SHARED USER STATE (mirror) ================================ | WIRED — reachable from server.js — Complete |
| `./redis/voiceState.js` | redis/voiceState.js ================================================== PHASE 2B-1 — CLUSTER-WIDE VOICE HEALTH (mirror) ===================== | WIRED — reachable from server.js — Complete |

## scripts/

| File | Purpose | Status |
|---|---|---|
| `./scripts/backup-data.js` | (no header comment) | WIRED — reachable from server.js — Complete |
| `./scripts/hydrate-from-db.js` | ================================================== PHASE 12 — BOOT-TIME HYDRATION FROM POSTGRES (additive, optional) ======================= | WIRED — reachable from server.js — Complete |
| `./scripts/production-preflight.js` | (no header comment) | WIRED — reachable from server.js — Complete |
| `./scripts/production-readiness.js` | (no header comment) | WIRED — reachable from server.js — Complete |
| `./scripts/restore-data.js` | (no header comment) | WIRED — reachable from server.js — Complete |
| `./scripts/sfu-load-test.js` | scripts/sfu-load-test.js ================================================== PHASE 3, STEP 3.6 — PRODUCTION LOAD-TEST TOOLING (spec item 3) = | WIRED (manual/ops CLI tool, documented in runbooks) — Complete |
| `./scripts/sfu-production-validate.js` | scripts/sfu-production-validate.js ================================================== PHASE 3, STEP 3.6 — PRODUCTION VALIDATION COMMAND (spe | WIRED (manual/ops CLI tool, documented in runbooks) — Complete |
| `./scripts/sfu-seatsync-retry-verify.js` | scripts/sfu-seatsync-retry-verify.js  Verification script for the 2026-08-10 root-cause fix: sync.js's onSeatChanged now retries once (bound | WIRED (manual/ops CLI tool, documented in runbooks) — Complete |
| `./scripts/sfu-step35-verify.js` | scripts/sfu-step35-verify.js Standalone verification script for PHASE3_STEP35_REPORT.md. Exercises rollout.js, provider.js, sync.js against  | WIRED (manual/ops CLI tool, documented in runbooks) — Complete |
| `./scripts/start-production.js` | (no header comment) | WIRED — reachable from server.js — Complete |

## security/

| File | Purpose | Status |
|---|---|---|
| `./security/bruteForce.js` | ================================================== PHASE 10 — BRUTE-FORCE PROTECTION (progressive lockout) ================================= | WIRED — reachable from server.js — Complete |
| `./security/corsConfig.js` | ================================================== MODULE 5.2 — ENV-DRIVEN CORS (shared by HTTP + Socket.IO) =============================== | WIRED — reachable from server.js — Complete |
| `./security/firebaseAuth.js` | ================================================== FIREBASE AUTH MIGRATION — PHASE A Firebase Admin SDK token verification (additive, self-c | WIRED — reachable from server.js — Complete |
| `./security/headers.js` | ================================================== PHASE 10 — SECURITY HEADERS (additive, no new dependency) =============================== | WIRED — reachable from server.js — Complete |
| `./security/rateLimiter.js` | ================================================== PHASE 10 — RATE LIMITING (in-memory, no new dependency) ================================= | WIRED — reachable from server.js — Complete |
| `./security/session.js` | ================================================== PHASE 10 — ADMIN SESSION MANAGEMENT (additive) ========================================== | WIRED — reachable from server.js — Complete |
| `./security/userAuth.js` | ================================================== PHASE 14 — USER SESSION TOKEN INFRASTRUCTURE (enforced on protected routes) ============= | WIRED — reachable from server.js — Complete |
| `./security/validation.js` | ================================================== PHASE 10 — INPUT VALIDATION & SANITIZATION ============================================== | WIRED — reachable from server.js — Complete |

## test/

| File | Purpose | Status |
|---|---|---|
| `./test/authHardening.test.js` | test/authHardening.test.js Verifies security/userAuth.js's requireUserAuth — the real middleware now gating the endpoints hardened in Module | WIRED (dynamically discovered by test/run-all.js) — Complete |
| `./test/callSignaling.test.js` | callSignaling.test.js Standalone verification harness — no npm deps, no real network/sockets. Mocks just enough of `io`/`socket` for callSig | WIRED (dynamically discovered by test/run-all.js) — Complete |
| `./test/corsConfig.test.js` | test/corsConfig.test.js Verifies security/corsConfig.js — the real module, not a re-implementation. No npm deps, no real HTTP server / Socke | WIRED (dynamically discovered by test/run-all.js) — Complete |
| `./test/crossInstanceUserEmit.test.js` | test/crossInstanceUserEmit.test.js Gap #1 (Redis Authoritative Runtime State) — REMAINING ITEM 1 verification.  Two things are exercised her | WIRED (dynamically discovered by test/run-all.js) — Complete |
| `./test/giftCrossInstanceEmit.test.js` | test/giftCrossInstanceEmit.test.js Gap #1 (Redis Authoritative Runtime State) — REMAINING ITEM 1/3 verification: the gift-triggered notifica | WIRED (dynamically discovered by test/run-all.js) — Complete |
| `./test/modActionRateLimit.test.js` | test/modActionRateLimit.test.js Verifies the rate-limit guard added in Module 5.2 for the previously- unthrottled host/admin moderation sock | WIRED (dynamically discovered by test/run-all.js) — Complete |
| `./test/paramsValidation.test.js` | test/paramsValidation.test.js Verifies security/validation.js's isSafeObjectKey() — the real function, not a re-implementation — added in Mo | WIRED (dynamically discovered by test/run-all.js) — Complete |
| `./test/productionInfrastructure.test.js` | (no header comment) | WIRED (dynamically discovered by test/run-all.js) — Complete |
| `./test/profileSecurityHardening.test.js` | test/profileSecurityHardening.test.js Static regression guard for the profile/ownership hardening pass. This intentionally checks the real s | WIRED (dynamically discovered by test/run-all.js) — Complete |
| `./test/redisAuthoritativeState.test.js` | test/redisAuthoritativeState.test.js Gap #1 (Redis Authoritative Runtime State) — verifies the cross-instance session read-through added to  | WIRED (dynamically discovered by test/run-all.js) — Complete |
| `./test/roomJoinRpc.test.js` | test/roomJoinRpc.test.js Gap #1 (Redis Authoritative Runtime State) — REMAINING ITEM 2 verification: cross-instance room join (redis/roomJoi | WIRED (dynamically discovered by test/run-all.js) — Complete |
| `./test/roomOpRpc.test.js` | test/roomOpRpc.test.js Gap #2 (Cross-Instance Room Operation Forwarding) verification: take-seat / leave-seat / send-message forwarding (red | WIRED (dynamically discovered by test/run-all.js) — Complete |
| `./test/roomStateJoinRace.test.js` | test/roomStateJoinRace.test.js Regression test for BUG-001 (forensic audit, 2026-08-10): a freshly joining socket never received its own ini | WIRED (dynamically discovered by test/run-all.js) — Complete |
| `./test/roomStateListRoomIds.test.js` | test/roomStateListRoomIds.test.js Gap #1 (Redis Authoritative Runtime State) — unit test for redis/roomState.js's new listRoomIds(), the clu | WIRED (dynamically discovered by test/run-all.js) — Complete |
| `./test/run-all.js` | test/run-all.js Small dependency-free test runner for the repository's standalone regression suites. Each suite is executed in its own Node  | WIRED — reachable from server.js — Complete |
| `./test/seatChangeVoiceRenegotiation.test.js` | seatChangeVoiceRenegotiation.test.js Regression test for the 2026-08-10 root-cause fix: seat changes were forcing an unnecessary SDP renegot | WIRED (dynamically discovered by test/run-all.js) — Complete |
| `./test/twoNodeCluster.test.js` | test/twoNodeCluster.test.js Gap #3 (Two-Node Cluster Validation) ================================================== Simulates a real two-ins | WIRED (dynamically discovered by test/run-all.js) — Complete |
| `./test/voiceCrossInstanceEmit.test.js` | test/voiceCrossInstanceEmit.test.js Gap #1 (Redis Authoritative Runtime State) — REMAINING ITEM 1 verification, voice-call-signaling case (c | WIRED (dynamically discovered by test/run-all.js) — Complete |
| `./test/voiceSfuLiveKitIntegration.test.js` | voiceSfuLiveKitIntegration.test.js Standalone verification harness, same convention as callSignaling.test.js (no npm deps, no real network,  | WIRED (dynamically discovered by test/run-all.js) — Complete |

## voice_sfu/

| File | Purpose | Status |
|---|---|---|
| `./voice_sfu/health.js` | voice_sfu/health.js ================================================== PHASE 3, STEP 3.2 — SFU HEALTH METRICS (EXTENDS, DOES NOT REPLACE) == | WIRED — reachable from server.js — Complete |
| `./voice_sfu/index.js` | voice_sfu/index.js ================================================== PHASE 3, STEP 3.2 — SFU MODULE ENTRY POINT (ADDITIVE) ================ | WIRED — reachable from server.js — Complete |
| `./voice_sfu/livekit.js` | voice_sfu/livekit.js ================================================== PHASE 3, STEP 3.2 — LIVEKIT SERVER-ADMIN CLIENT ==================== | WIRED — reachable from server.js — Complete |
| `./voice_sfu/provider.js` | voice_sfu/provider.js ================================================== PHASE 3, STEP 3.2 — VOICE PROVIDER ABSTRACTION ==================== | WIRED — reachable from server.js — Complete |
| `./voice_sfu/rollout.js` | voice_sfu/rollout.js ================================================== PHASE 3, STEP 3.5 — STAGED ROLLOUT DECISION ENGINE (NEW, ADDITIVE) = | WIRED — reachable from server.js — Complete |
| `./voice_sfu/roomManager.js` | voice_sfu/roomManager.js ================================================== PHASE 3, STEP 3.2 — SFU ROOM MAPPING =========================== | WIRED — reachable from server.js — Complete |
| `./voice_sfu/startupCheck.js` | voice_sfu/startupCheck.js ================================================== PHASE 3, STEP 3.5 — PRODUCTION READINESS / STARTUP VALIDATION ( | WIRED — reachable from server.js — Complete |
| `./voice_sfu/sync.js` | voice_sfu/sync.js ================================================== PHASE 3, STEP 3.4 — SERVER <-> LIVEKIT ROOM/PARTICIPANT LIFECYCLE SYNC  | WIRED — reachable from server.js — Complete |
| `./voice_sfu/token.js` | voice_sfu/token.js ================================================== PHASE 3, STEP 3.2 — SFU ACCESS TOKEN GENERATION ====================== | WIRED — reachable from server.js — Complete |

## Non-JS critical files

| File | Purpose | Status |
|---|---|---|
| `docker-compose.production.yml` | Production compose stack | Needs Phase 12 review |
| `Dockerfile` | Container build | Needs Phase 12 review |
| `nginx.conf.example` | Reverse proxy template w/ WS upgrade headers | Needs Phase 12 review |
| `.env.example` | Documented env vars incl. `MODULE4_WALLET_ENABLED=false` (confirms deferred wallet cutover is deliberate, matches code) | Complete (as template) |
| `ecosystem.config.js` | PM2 fork-mode config | Needs Phase 12 review |
| `.github/workflows/ci.yml` | CI: `npm ci` → `node --check server.js` → `npm test` → `production-preflight.js` | **GAP: does not run `integration_update/*/test/*.js` (94 tests exist and pass standalone but are invisible to CI)** |
| `integration_update/country_permission/migrations/001_*.sql` | Country permission schema | Needs Phase 2/7 review |
| `integration_update/merchant/migrations/001_*.sql` | Merchant schema | Needs Phase 2/7 review |
| `integration_update/module4_wallet_ledger/migrations/001_*.sql` | Module 4 wallet ledger schema | Needs Phase 7 review — never executed against real Postgres (by design, no live DB yet) |
| `integration_update/module4_wallet_ledger/wallet/schema.sql` | Wallet table definitions | Needs Phase 7 review |
| `monitoring/prometheus.yml`, `prometheus-alerts.yml`, `grafana/**` | Observability stack config | Needs Phase 10 review |
