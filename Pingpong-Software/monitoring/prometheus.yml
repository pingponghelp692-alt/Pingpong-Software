global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/prometheus-alerts.yml
scrape_configs:
  - job_name: pingpong
    metrics_path: /api/metrics
    static_configs:
      - targets: ['pingpong:3000']
